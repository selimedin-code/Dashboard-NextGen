# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20

# Config (loosened lower-bounds so Python 3.14 gets prebuilt wheels; see requirements.lock.txt for pinned set)
pydantic>=2.12
pydantic-settings>=2.7

# Database (psycopg3 — best support on Python 3.13+)
sqlalchemy>=2.0.43
alembic==1.14.0
psycopg[binary]==3.2.13

# Data ingest (Phase 1 — Excel/PDF parsing)
openpyxl==3.1.5
pdfplumber==0.11.4

# HTTP client for external data sources (later phases)
httpx==0.28.1
